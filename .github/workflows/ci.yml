name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Read target version
        id: version
        run: echo "version=$(cat VERSION)" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: clickhouse-backup-doppler:test
          build-args: |
            CLICKHOUSE_BACKUP_VERSION=${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify clickhouse-backup binary
        run: docker run --rm --entrypoint clickhouse-backup clickhouse-backup-doppler:test --version

      - name: Verify doppler CLI
        run: docker run --rm --entrypoint doppler clickhouse-backup-doppler:test --version
